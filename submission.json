{
    "config": ["config/data-params.json", "config/figures.json"],  // paths of at least 2 config files
    "build": "run.py", // path of your build script
    "library": ["src"], // directory containing (your) library code
    "notebook": ["notebooks/text.ipynb"] // path to at least one notebook meant for reading by someone other than you.
    "targets": ["eda", "notebook"] // at least two targets
}
